spring:
   datasource:
      url: jdbc:postgresql://${db.domain}/analysis?currentSchema=public
      username: ${db.user}
      password: ${db.password}
      driverClassName: org.postgresql.Driver
   jpa:
      show-sql: true
      hibernate:
         ddl-auto: none
      properties:
         hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
   liquibase:
      change-log: classpath:/db/changelog/postgresql/changelogMaster.xml
      contexts: ${spring.profiles.active}
      dropFirst: true
server:
   port: 9090
   
services:
   security: 
      enabled: false
      uri: http://localhost:9092/rws-security

sdc:
   dictionary:
      secret:
         git-authorization-type: token
         git-authorization: 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
         generic-authorization: ghp_GenericT1LU4Mh5HhoxckHaK0LJ1ev2pK4CU
         